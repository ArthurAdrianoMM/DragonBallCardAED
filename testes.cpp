#include "mao.h"
#include "monstros.h"
#include "mapa.h"


// Atualize a função principal para incluir o descarte
int main() {   

    tp_pilha descarte;
    tp_pilha deck;

    

     /*
    Carta teste;
    
    criar_deck(&deck);

    Mao mao = criar_mao();
    Descarte descarte = criar_descarte();

    for (int i = 0; i < 5; i++) {
        adicionar_carta(&mao, &deck, 1);
    }

    mostrar_mao(mao);
    jogar_carta(&mao, &deck, &descarte);

    Quando todas as cartas do deck e da mão estiverem no descarte, embaralhe o descarte no deck
    if (mao.tamanho == 0 && is_empty(&deck)) {
        embaralhar_descarte(&descarte, &deck);
    }
    */

 
    return 0;
}
